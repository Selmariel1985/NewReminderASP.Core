@{
    ViewBag.Title = "Role List";
}

<h2>Role List</h2>

<table>
    <tr>
        <th>ID</th>
        <th>Name</th>

    </tr>
    @foreach (var role in Model)
    {
        <tr>
            <td>@role.Id</td>
            <td>@role.Name</td>
        </tr>
    }
</table>